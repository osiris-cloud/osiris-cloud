import {
    select,
    selectAll,
    arc,
    pie,
    interpolate,
    scaleLinear,
    scaleThreshold
} from 'd3';

window.d3 = {select, selectAll, arc, pie, interpolate, scaleLinear, scaleThreshold};
